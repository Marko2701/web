import React, { Component } from 'react';
import "./Header.css"

class Header extends Component {
    render() {
        return (
            <header>
                <span>MARVEL</span>
            </header>
        );
    }
}

export default Header;